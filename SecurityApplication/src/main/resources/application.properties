spring.application.name=SecurityApplication


server.port=8060

spring.datasource.url=jdbc:mysql://localhost:3306/SpringSecurity?useSSL=false
spring.datasource.username=root
spring.datasource.password=
spring.jpa.hibernate.ddl.auto=update
spring.jpa.generate-ddl=true
spring.jpa.show.sql=true
spring.jpa.properties.hibernate.format_sql=true


# defining our own username and password
#spring.security.user.name = anmol
#spring.security.user.password = pass



#creating the secretKey for JWT
#It needs to be a long Key(lengthwise not datatype) because the algorithm which we are using need 256bits key so 256bits means 32 bytes and
# this could be random key even bigger than 32bytes because more bigger the length , less will be the chances of guessing the secretKey.
jwt.secretKey=anjdnjadksjanfdkjks556fnsabfdanflwqdkq78qtewuk76dfsmknjnscsdjns23dwjnjd
